import { Lowercase1Pipe } from './lowercase1.pipe';

describe('Lowercase1Pipe', () => {
  it('create an instance', () => {
    const pipe = new Lowercase1Pipe();
    expect(pipe).toBeTruthy();
  });
});
